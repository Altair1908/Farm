package lesson7.homeani;

import lesson7.Home;

public class Cat extends Home {
    public Cat(int weight, int moveSpeed, String name, int health, int resource, boolean eatable) {
        super(weight, moveSpeed, name, health, resource, eatable);
    }
}
