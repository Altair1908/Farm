package lesson7.homeani;

import lesson7.Home;

public class Cat extends Home {
    public Cat(String name, int weight, int moveSpeed, int health, int resource, boolean eatable) {
        super(name, weight, moveSpeed, health, resource, eatable);
    }
}
